# Template: copy this file into the target repository as
# .github/workflows/rdp.yml so the Discord bot can create a temporary
# Windows RDP machine with /rdp create. The workflow:
#   1. enables Remote Desktop on the runner,
#   2. creates a local administrator with a random password,
#   3. opens a free pinggy.io TCP tunnel to port 3389 in the FINAL step
#      (GitHub's Windows runner kills step-spawned background processes
#      when the step ends, so the tunnel must own the last step),
#   4. uploads Address/Username/Password as the rdp-creds-<run_id> /
#      rdp-info-<run_id> artifacts (readable while the run is in progress)
#      and optionally POSTs the address to the notify_url webhook input,
#   5. keeps the machine alive until the run is cancelled or GitHub's
#      6-hour hosted-runner cap ends the job.
#
# NOTE: pinggy free tunnels expire after 60 minutes — create a new machine
# with /rdp create when that happens. No repository secrets are required.
#
# WARNING: GitHub Actions terms only cover project build/test workloads.
# Using hosted runners as personal remote desktops can get the account
# suspended — use your own token, at your own risk.

name: Windows RDP Machine

on:
  workflow_dispatch:
    inputs:
      username:
        description: "Windows username for the RDP session"
        required: false
        default: "rdpadmin"
      notify_url:
        description: "Optional webhook URL to POST the tunnel address to"
        required: false
        default: ""

permissions:
  contents: read

jobs:
  rdp:
    name: Windows RDP machine
    runs-on: windows-latest
    timeout-minutes: 350

    steps:
      - name: Enable Remote Desktop
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name 'fDenyTSConnections' -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name 'UserAuthentication' -Value 0
          Enable-NetFirewallRule -DisplayGroup 'Remote Desktop'
          Restart-Service TermService -Force -ErrorAction SilentlyContinue
          # Self-check: wait until something is actually listening on 3389.
          $listening = $false
          for ($i = 0; $i -lt 15; $i++) {
            Start-Sleep -Seconds 2
            $r = Test-NetConnection -ComputerName 127.0.0.1 -Port 3389 -WarningAction SilentlyContinue
            if ($r.TcpTestSucceeded) { $listening = $true; break }
          }
          if (-not $listening) {
            Write-Host "::error::Nothing is listening on RDP port 3389 after enabling Remote Desktop."
            exit 1
          }
          Write-Host "Remote Desktop enabled — listener confirmed on 127.0.0.1:3389."

      - name: Create RDP user with random password
        shell: pwsh
        env:
          RDP_USERNAME_INPUT: ${{ inputs.username }}
        run: |
          # NB: avoid 'runneradmin' — the GitHub runner image ships that
          # account and its password policy blocks a plain reset.
          $username = if ($env:RDP_USERNAME_INPUT) { $env:RDP_USERNAME_INPUT } else { 'rdpadmin' }

          function Get-RandomString {
            param([string]$Pool, [int]$Length)
            $bytes = New-Object byte[] $Length
            [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
            -join ($bytes | ForEach-Object { $Pool[$_ % $Pool.Length] })
          }

          # Complex by construction (upper+lower+digit+special), then shuffled
          # so the character classes are not position-predictable.
          $chars = @()
          $chars += (Get-RandomString -Pool 'ABCDEFGHJKLMNPQRSTUVWXYZ'  -Length 2).ToCharArray()
          $chars += (Get-RandomString -Pool 'abcdefghijkmnpqrstuvwxyz' -Length 10).ToCharArray()
          $chars += (Get-RandomString -Pool '23456789'                 -Length 3).ToCharArray()
          $chars += (Get-RandomString -Pool '!#$%*+-.?@^~'             -Length 1).ToCharArray()
          for ($i = $chars.Length - 1; $i -gt 0; $i--) {
            $j = Get-Random -Maximum ($i + 1)
            $chars[$i], $chars[$j] = $chars[$j], $chars[$i]
          }
          $password = -join $chars

          net user $username $password /add /y
          if ($LASTEXITCODE -ne 0) {
            # If the account already exists, reset its password instead.
            net user $username $password /y
            if ($LASTEXITCODE -ne 0) {
              Write-Host "::error::Failed to create or update the Windows user."
              exit 1
            }
          }
          net localgroup Administrators $username /add
          net localgroup 'Remote Desktop Users' $username /add

          # Hand the credentials to the artifact steps; never echo them here.
          Add-Content -Path $env:GITHUB_ENV -Value "RDP_USERNAME=$username"
          Add-Content -Path $env:GITHUB_ENV -Value "RDP_PASSWORD=$password"
          Write-Host "User $username created (password withheld from logs)."
          Set-Content -Path 'rdp-creds.txt' -Value @(
            "Username: $username",
            "Password: $password"
          )

      - name: Upload credentials artifact
        uses: actions/upload-artifact@v4
        with:
          name: rdp-creds-${{ github.run_id }}
          path: rdp-creds.txt
          if-no-files-found: error
          retention-days: 1

      - name: Start pinggy tunnel, publish info, keep alive
        shell: pwsh
        env:
          NOTIFY_URL: ${{ inputs.notify_url }}
        run: |
          # The tunnel MUST live inside this final step: GitHub's Windows
          # runner kills step-spawned background processes when the step
          # ends, so a tunnel started in an earlier step is dead by the
          # time anyone tries to connect.
          $sshExe = Join-Path $env:WINDIR 'System32\OpenSSH\ssh.exe'
          if (-not (Test-Path $sshExe)) {
            $sshExe = Join-Path $env:ProgramFiles 'Git\usr\bin\ssh.exe'
          }
          if (-not (Test-Path $sshExe)) {
            Write-Host "::error::ssh.exe not found on the runner."
            exit 1
          }
          $pinggyOut = Join-Path $env:RUNNER_TEMP 'pinggy.out'
          $pinggyErr = Join-Path $env:RUNNER_TEMP 'pinggy.err'
          $proc = Start-Process -FilePath $sshExe `
            -ArgumentList '-n', '-T', '-o', 'StrictHostKeyChecking=no', '-o', 'ConnectTimeout=30', `
              '-p', '443', '-R0:localhost:3389', 'tcp@free.pinggy.io' `
            -RedirectStandardOutput $pinggyOut -RedirectStandardError $pinggyErr `
            -WindowStyle Hidden -PassThru

          $address = $null
          for ($i = 0; $i -lt 60; $i++) {
            Start-Sleep -Seconds 2
            if ($proc.HasExited) { break }
            foreach ($logFile in @($pinggyOut, $pinggyErr)) {
              if ((Test-Path $logFile) -and -not $address) {
                $match = Select-String -Path $logFile -Pattern 'tcp://([a-z0-9.-]+):(\d+)' |
                  Select-Object -First 1
                if ($match) {
                  $address = $match.Matches[0].Groups[1].Value + ':' + $match.Matches[0].Groups[2].Value
                }
              }
            }
            if ($address) { break }
          }
          if (-not $address) {
            Write-Host "=== pinggy stdout ==="
            if (Test-Path $pinggyOut) { Get-Content $pinggyOut | ForEach-Object { Write-Host $_ } }
            Write-Host "=== pinggy stderr ==="
            if (Test-Path $pinggyErr) { Get-Content $pinggyErr | ForEach-Object { Write-Host $_ } }
            Write-Host "::error::Could not obtain the pinggy tunnel address."
            exit 1
          }

          Set-Content -Path 'rdp-info.txt' -Value @(
            "Address:  $address",
            "Tunnel:   pinggy-free (direct TCP, no client needed; 60 min limit)",
            "Username: $env:RDP_USERNAME",
            "Password: $env:RDP_PASSWORD"
          )

          # Channel that needs no GitHub permissions: POST the address to the
          # caller-provided webhook (no secrets in the body).
          if ($env:NOTIFY_URL) {
            try {
              Invoke-RestMethod -Method Post -Uri $env:NOTIFY_URL -ContentType 'text/plain' `
                -Body "rdp-machine`nhost=$address`nuser=$env:RDP_USERNAME" | Out-Null
              Write-Host "Notify webhook called."
            } catch {
              Write-Host "::warning::Notify webhook failed: $_"
            }
          }

          Write-Host ""
          Write-Host "================ RDP CONNECTION INFO ================"
          Write-Host "TUNNEL_ADDRESS=$address"
          Write-Host "RDP_USER=$env:RDP_USERNAME"
          Write-Host "====================================================="
          Write-Host "Tunnel ready: $address — holding the session."
          while ($true) {
            Start-Sleep -Seconds 60
            if ($env:NOTIFY_URL) {
              $probe = @()
              $probe += "heartbeat=$(Get-Date -Format o)"
              $probe += "pinggy_alive=$(-not $proc.HasExited)"
              $r = Test-NetConnection -ComputerName 127.0.0.1 -Port 3389 -WarningAction SilentlyContinue
              $probe += "rdp_listener=$($r.TcpTestSucceeded)"
              try {
                Invoke-RestMethod -Method Post -Uri $env:NOTIFY_URL -ContentType 'text/plain' `
                  -Body ($probe -join "`n") | Out-Null
              } catch { }
            }
          }

      - name: Upload connection info artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rdp-info-${{ github.run_id }}
          path: rdp-info.txt
          if-no-files-found: ignore
          retention-days: 1
